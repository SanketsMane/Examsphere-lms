# Assets — drop client-supplied images here

Put the actual images the client wants used, so implementation can reference them.

Required:
- **`hero-students.*`** — the boy + girl student photo for the homepage hero (Phase 1).
  The client said "same boy and girl in the hero section." Until the real file is here, the
  hero uses a labelled placeholder. Final home for it in the app: `public/images/`.

Optional / nice to have:
- Logo variants, favicon, any brand illustrations.
- Screenshot of the client's reference image (the one shared over WhatsApp) for side-by-side QA.
